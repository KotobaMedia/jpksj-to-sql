---
description:
globs:
alwaysApply: true
---
# Rust Coding Style

- Avoid nesting. For example, instead of `if let Some(x) = y`, we can do `let Some(x) = y else { return Err(...) }` to return early and keep the nest count down.
